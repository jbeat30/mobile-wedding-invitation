'use client';

import { OverviewCard } from '@/app/(admin)/admin/components/OverviewCard';

/**
 * 요약 섹션
 * @returns JSX.Element
 */
export const AdminSectionOverview = () => {
  return <OverviewCard />;
};
